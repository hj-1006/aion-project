[defaults]
inventory = inventory/hosts.ini
# 네트워크 장비 SSH: 처음 접속 시 호스트 키 확인 건너뜀 (장비가 많을 때 사용)
host_key_checking = False
# CentOS 9 / Python 인터프리터 deprecation 경고 숨김
deprecation_warnings = False

[persistent_connection]
# show memory 등 응답이 느린 명령용 (기본 30초 → 90초)
command_timeout = 10
