# AION - 마이크로서비스 환경 변수
NODE_ENV=development

# MySQL (query-server 전용)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=aion
MYSQL_PASSWORD=aion_secret
MYSQL_DATABASE=aion

# Query Server (포트 3002)
# API 서버가 쿼리 서버 호출 시 사용
QUERY_SERVER_URL=http://localhost:3002

# API Server (포트 3001)
PORT=3001
SESSION_SECRET=change_this_in_production
SYSLOG_PORT=5514
PROMETHEUS_METRICS_ENABLED=true
TELEMETRY_POLL_INTERVAL_MS=60000
SNMP_COMMUNITY=public
# LLM (Ollama): 로컬 테스트 시 원격 서버 예 → OLLAMA_URL=http://10.100.0.200:11434 (docs/LOCAL-TEST.md, docs/LLM-OLLAMA.md)
OLLAMA_URL=http://10.200.200.200:11434
OLLAMA_MODEL=llama3
# OLLAMA_DISABLED=true  # LLM 비활성화 시 (연결 실패 시 기본 명령만 사용)

# Web Server 포트 (기본 3000, 80 사용 시 WEB_PORT=80 → root 또는 setcap 필요, docs/RUN-WITHOUT-DOCKER.md)
# WEB_PORT=80
# API 프록시 대상
API_SERVER_URL=http://localhost:3001

# 메일 (내부망 SMTP) - 메일 발송 페이지에서 사용
# SMTP_HOST=mail.example.local
# SMTP_PORT=25
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# MAIL_FROM=aion@example.local
